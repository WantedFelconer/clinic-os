const authValidator = require('./authValidator');
const appointmentValidator = require('./appointmentValidator');
const medicalRecordValidator = require('./medicalRecordValidator');
const medicalReportValidator = require('./medicalReportValidator');
const prescriptionValidator = require('./prescriptionValidator');
const paymentValidator = require('./paymentValidator');
const reviewValidator = require('./reviewValidator');
const clinicValidator = require('./clinicValidator');
const paginationValidator = require('./paginationValidator');
const doctorValidator = require('./doctorValidator');

module.exports = {
  authValidator,
  appointmentValidator,
  medicalRecordValidator,
  medicalReportValidator,
  prescriptionValidator,
  paymentValidator,
  reviewValidator,
  clinicValidator,
  paginationValidator,
  doctorValidator,
};
